<%= simple_form_for :votes, url: {action: 'create'}, :html => { :multipart => true } do |f| %>
<h2>Stimmkarten erstellen</h2>
<p>Du bist dabei, Stimmkarten für die Abstimmung "<%= @voting.title %>" anzulegen.</p>
<%= f.input :max_choices, as: :integer, label: 'Anzahl der Stimmen je Wahlkarte (>1 für Multiple-Choice)' %>
<hr />
<p>Die Wahlkarten werden an alle <%= User.all.count %> Nutzer von abimorrow.net gesendet.</p>
<%= f.input :notify, as: :boolean, label: "per E-Mail benachrichtigen?" %>
<%= f.button :submit, raw("Stattdessen an eine Auswahl senden &raquo;"), class: 'btn btn-warning' %>
<%= if @voting.votes.empty? then f.button :submit, raw("Erstellen &raquo;"), class: 'btn-primary', data: { confirm: "Möchtest du wirklich Wahlkarten versenden? Diese Aktion lässt sich nicht rückgängig machen."}, name: :commit, value: 'Erstellen' end %>
<% end %>
