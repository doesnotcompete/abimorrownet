<table id="teachers-table" class="table table-striped table-bordered" data-source="<%= teachers_path(format: :json) %>">
  <thead>
    <tr>
      <th>Vorname</th>
      <th>Nachname</th>
      <th>Aktion</th>
    </tr>
  </thead>
  <tbody>
  </tbody>
</table>
<hr />
<%= link_to raw("<span class='glyphicon glyphicon-plus' aria-hidden='true'></span> Lehrer erstellen"), new_teacher_path, class: 'btn btn-sm btn-success' %>
