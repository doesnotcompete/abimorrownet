<table id="profiles-table" class="table table-striped table-bordered" data-source="<%= profiles_path(format: :json) %>">
  <thead>
    <tr>
      <th>Vorname</th>
      <th>Nachname</th>
      <th>Seminarfach</th>
      <th>Aktion</th>
    </tr>
  </thead>
  <tbody>
  </tbody>
</table>
<% if current_user.moderator? || current_user.admin? %>
<%= link_to 'Verteilerliste herunterladen', profiles_path(format: :csv) %>
<% end %>
